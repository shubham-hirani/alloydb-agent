apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: eco-matcher-agent
  annotations:
    run.googleapis.com/launch-stage: BETA
spec:
  template:
    spec:
      serviceAccountName: 610755018887-compute@developer.gserviceaccount.com
      containers:
        - image: BACKEND_IMAGE
          name: backend
          resources:
            limits: { cpu: "1", memory: "512Mi" }
          # No ports defined here is correct for sidecars
        - image: FRONTEND_IMAGE
          name: frontend
          ports:
            - containerPort: 8080
          env:
            - name: BACKEND_URL
              value: "http://localhost:8000/chat"
          startupProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
      timeoutSeconds: 300